# AGENTS.md

이 문서는 이 저장소에서 작업하는 코딩 에이전트의 기본 지침입니다.

## 프로젝트 개요

- 이 저장소는 NEIS 공개 API를 활용해 학교 급식 메뉴를 조회하고 분석하는
  웹 애플리케이션 개발 워크숍입니다.
- 현재 저장소는 워크숍 문서와 API 원본 자료를 중심으로 구성되어 있으며,
  프론트엔드, 백엔드, 배포 코드는 단계적으로 추가될 수 있습니다.
- 요청된 변경 범위 안에서만 작업하고 관련 없는 코드, 문서, 설정은 변경하지
  않습니다.

## 일반 작업 지침

- 사용자 요청과 인수 조건을 먼저 확인하고, 구현 전에 관련 파일과 기존 패턴을
  살펴봅니다.
- 더 구체적인 경로의 `AGENTS.md`, 저장소 문서, 구성 파일, CI 설정 등 기존
  프로젝트 규칙을 이 문서보다 우선합니다.
- 기존 구조와 명명 규칙을 따르고 필요한 최소 범위로 변경합니다.
- 현재 요구사항에 필요하지 않은 추상화, 범용 프레임워크, 사전 최적화 또는
  대규모 리팩터링을 추가하지 않습니다.
- 동작을 변경할 때는 호출부, 테스트, 구성 및 문서에 미치는 영향을 함께
  확인합니다.
- 생성 파일이나 외부 데이터는 원본을 직접 수정하기보다 생성 절차 또는
  원천을 수정합니다.

## Python 가이드라인

- Python 코드는 [PEP 8](https://peps.python.org/pep-0008/)을 기본 스타일
  가이드로 따르되, 저장소에 구성된 포맷터와 린터 규칙이 있으면 이를 우선합니다.
- 공개 함수, 메서드 및 데이터 구조에는 구체적인 타입을 사용하고 불필요한
  `Any`와 무분별한 타입 무시는 피합니다.
- 예상 가능한 오류를 구체적인 예외로 처리하고, 광범위한 예외 포착이나 오류를
  숨기는 기본값을 사용하지 않습니다.
- 입력과 외부 API 응답은 신뢰 경계에서 검증하고, 실패 원인을 호출자에게
  명확히 전달합니다.
- 의존성은 프로젝트가 사용하는 선언 파일과 잠금 정책을 따르며, 표준
  라이브러리나 기존 의존성으로 해결할 수 있으면 새 패키지를 추가하지
  않습니다.
- 작은 단위의 명확한 함수, 일관된 명명, 불변 데이터 우선 등 읽기 쉽고
  테스트 가능한 코드를 작성합니다.

## TypeScript 가이드라인

- TypeScript 코드는 적용 가능한 범위에서
  [Mozilla 코딩 스타일 가이드](https://firefox-source-docs.mozilla.org/code-quality/coding-style/index.html)를
  따르되, 저장소의 기존 스타일과 포맷터 및 린터 설정을 우선합니다.
- 엄격한 타입 검사를 전제로 구체적인 타입을 사용하고, 불필요한 `any`, 타입
  단언 및 non-null assertion을 피합니다.
- `unknown` 입력은 타입 가드 또는 스키마를 통해 좁힌 후 사용하며, API 경계의
  요청과 응답을 검증합니다.
- 비동기 오류를 삼키지 말고 사용자 또는 호출자가 처리할 수 있는 형태로
  전달합니다.
- 의존성은 기존 패키지 관리자와 잠금 파일을 사용해 관리하고, 새 의존성은
  필요성과 유지보수 영향을 확인한 뒤 추가합니다.
- 기존 모듈과 컴포넌트 패턴을 따르고, 중복을 줄이되 단일 사용 사례를 위한
  불필요한 추상화는 만들지 않습니다.

## 테스트 및 유효성 검사

- 변경된 동작에는 정상 경로와 중요한 실패 경로를 다루는 테스트를 추가하거나
  수정합니다.
- 가장 작고 관련성 높은 검사부터 실행하며, 필요할 때만 검사 범위를 넓힙니다.
- 저장소에 실제로 구성된 포맷터, 린터, 타입 검사기, 테스트 및 빌드 명령만
  사용합니다. 구성 파일, 패키지 스크립트, `CONTRIBUTING.md`와 CI 설정에서
  명령을 확인하며, 아직 구성되지 않은 도구나 명령을 임의로 추가하거나
  가정하지 않습니다.
- 실행하지 못한 검사나 남아 있는 실패는 완료 결과에 명확히 기록합니다.

## 보안

- 비밀, 자격 증명, 토큰, 개인 정보 및 민감한 운영 데이터를 코드, 로그,
  테스트 픽스처 또는 문서에 포함하지 않습니다.
- 외부 입력을 검증하고 출력 또는 쿼리 컨텍스트에 맞게 안전하게 처리하며,
  최소 권한 원칙을 따릅니다.
- 새 의존성, 네트워크 접근 및 권한 변경은 꼭 필요한 경우로 제한합니다.
- 보안 취약점은 공개 이슈에 기록하지 않고 `SECURITY.md`의 비공개 신고 절차를
  따릅니다.

## 문서화

- 사용자 동작, 공개 API, 설정, 배포 또는 개발 절차가 바뀌면 관련 문서를 같은
  변경에 포함합니다.
- 문서는 현재 구현과 일치하게 유지하고, 확인되지 않은 명령이나 기능을
  지원되는 것처럼 설명하지 않습니다.
- 코드 자체로 의도가 명확하지 않은 경우에만 간결한 주석을 추가합니다.

## 주의사항 및 가드레일

- 요청 없이 기존 변경을 되돌리거나 파일을 삭제하지 않습니다.
- 파괴적인 Git 명령, 대규모 자동 치환 및 범위를 알 수 없는 삭제를 피합니다.
- 하위 호환성을 우선하며 의도적인 호환성 중단은 명시적인 승인과 문서화를
  거칩니다.
- 테스트를 통과시키기 위해 검증을 약화하거나 오류를 숨기거나 타입 안정성을
  우회하지 않습니다.
- 요구사항이 모호하고 선택에 따라 동작 또는 구조가 크게 달라지면 구현 전에
  확인합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 `CONTRIBUTING.md`의 Conventional
  Commits 규칙을 따릅니다.
- 관련 없는 변경이나 생성 파일을 커밋에 섞지 않으며 비밀 정보 포함 여부를
  확인합니다.
- Pull Request는 `.github/PULL_REQUEST_TEMPLATE.md`를 사용해 변경 이유,
  관련 이슈, 변경 유형, 검증 내용과 체크리스트를 빠짐없이 작성합니다.
- PR은 검토 가능한 크기로 유지하고, 동작 변경에는 적절한 테스트와 문서를
  포함하며, 알려진 제한이나 실행하지 못한 검사를 명시합니다.
